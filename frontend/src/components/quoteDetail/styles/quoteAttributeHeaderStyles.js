import styled from 'styled-components';

export const QuoteAttributeHeaderStyles = styled.div`
    width: 100%;
    background-color: ${props => props.backgroundColor}; 
    color: white; 
    padding: 1%;
    font-weight: bold; 
    margin-bottom: 10px;
    box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
`;